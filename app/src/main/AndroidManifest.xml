<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.karelsalcedo.subnormalcheckin">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controladores.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controladores.PreviewActivity" />
        <activity android:name=".Controladores.HelpActivity" />
        <activity android:name=".Controladores.ReviewActivity" />
        <activity android:name=".Controladores.SettingsActivity" />
        <activity android:name=".Controladores.DataActivity" />
        <activity android:name=".Controladores.AboutActivity" />
        <activity android:name=".CheckOk"></activity>
    </application>

</manifest>